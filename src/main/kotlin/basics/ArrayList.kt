package basics

class ArrayList {

    // Unlike lists, which have mutable and immutable versions, there is no mutable version of an Array

    fun test(){
        arrayOf("") //mutable always
        listOf<String>("")
        mutableListOf("")
        intArrayOf(1,2)

    }

}